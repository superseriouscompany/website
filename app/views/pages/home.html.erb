<section class="context">
  <p>
    Plot: Homeless guy and his boss quit jobs and randomly move to Portugal.
  </p>
</section>

<section class="story">
  <% day = @day %>
  <% cache day do %>
    <article class="day">
      <%= render day %>
    </article>
  <% end %>

  <div class="readMore">
    <a href="/story">Read the full story</a>
  </div>
</section>

<%= render "layouts/social" %>
